server:
  port: 8081

management:
  info:
    git:
      mode: full
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  level.root: INFO
  #  level.org.hibernate.SQL: DEBUG
  level.liquibase: INFO
  level.io.fdlessard.codebites.debezium: DEBUG

spring:
  application:
    name: customer-listener
  output:
    ansi:
      enabled: always
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  jackson:
    default-property-inclusion: non_null
    time-zone: UTC
    serialization:
      write_dates_as_timestamps: false
  kafka:
    bootstrap-servers: kafka-server:9092
  cloud:
    stream:
      bindings:
        receiveCustomer-in-0:
          destination: my-connect-configs
          group: cdcGroup
          contentType: application/json
    function:
      definition: receiveCustomer